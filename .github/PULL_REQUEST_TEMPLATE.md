## Summary
What does this PR do?

## Changes
- List the main changes, e.g.:
  - contract: added `mutual_refund` with auth guards
  - frontend: live event stream polling
  - ci: added `wasm32v1-none` build check

## Testing
- [ ] `cargo test --release` passes
- [ ] `cargo build --release --target wasm32v1-none` succeeds
- [ ] `cd frontend && npm test` passes
- [ ] `npm run build` succeeds

## Notes for reviewers
Anything unusual, migration steps, or contract redeploy requirements?
